<h1>New Genre</h1>

<%= form_for(@genre) do |f| %>

  <%= f.label :name, "My genre name" %>
  <%= f.text_field :name %><br>
  <%= f.label :bio, "Bio:" %><br>
  <%= f.text_area :bio, size: "60x12" %><br>

  <%= f.submit "Create Genre" %>

<% end %>
